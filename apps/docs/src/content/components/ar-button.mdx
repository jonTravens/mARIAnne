---
tagName: ar-button
title: Button
description: Bouton accessible et personnalisable via CSS custom properties.
playgroundTemplate: filled
variants:
    - name: filled
      label: Filled
      description: Variante par défaut, fond coloré.
      html: |
          <ar-button>Filled</ar-button>

    - name: outlined
      label: Outlined
      description: Contour sans fond, pour les actions secondaires.
      html: |
          <ar-button variant="outlined">Outlined</ar-button>

    - name: ghost
      label: Ghost
      description: Sans fond ni contour, pour les actions tertiaires.
      html: |
          <ar-button variant="ghost">Ghost</ar-button>

    - name: danger
      label: Danger
      description: Action destructrice ou irréversible.
      html: |
          <ar-button variant="danger">Danger</ar-button>

    - name: sizes
      label: Tailles
      description: Les trois tailles disponibles côte à côte.
      html: |
          <div style="display:flex; align-items:center; gap:1rem; flex-wrap:wrap;">
            <ar-button size="small">Small</ar-button>
            <ar-button size="medium">Medium</ar-button>
            <ar-button size="large">Large</ar-button>
          </div>

    - name: disabled
      label: Disabled
      description: État désactivé — aucun événement n'est émis.
      html: |
          <ar-button disabled>Disabled</ar-button>

    - name: icons
      label: Avec icônes
      description: Utilisation des slots prefix et suffix.
      html: |
          <div style="display:flex; gap:1rem; flex-wrap:wrap;">
            <ar-button>
              <span slot="prefix">←</span>
              Retour
            </ar-button>
            <ar-button>
              Suivant
              <span slot="suffix">→</span>
            </ar-button>
          </div>

    - name: all
      label: Toutes les variantes
      description: Aperçu complet de toutes les combinaisons.
      html: |
          <div style="display:flex; gap:1rem; flex-wrap:wrap; align-items:center;">
            <ar-button>Filled</ar-button>
            <ar-button variant="outlined">Outlined</ar-button>
            <ar-button variant="ghost">Ghost</ar-button>
            <ar-button variant="danger">Danger</ar-button>
            <ar-button disabled>Disabled</ar-button>
          </div>
---

Le composant `ar-button` s'adapte à tous les contextes via ses variantes et ses CSS custom properties.
Il émet `ar-click`, `ar-focus` et `ar-blur` au lieu des événements natifs, ce qui permet de filtrer
les interactions disabled côté applicatif sans logique supplémentaire.
